name: Content Deployment

on:
  push:
    branches: [ main ]

jobs:
  staging:
    name: Staging
    runs-on: ubuntu-latest
    steps:
      - name: Echo staging step
        run: echo "Staging step (dummy)"

  production:
    name:  Production
    runs-on: ubuntu-latest
    needs: staging
    environment:
      name: production
      # Requires manual approval in the GitHub UI by environment approvers for "production"
      # Set up required reviewers in your repository settings for this environment
    steps:
      - name: Echo production step
        run: echo "Production step (dummy)"
